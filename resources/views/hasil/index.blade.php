<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold leading-tight text-slate-800">Hasil Ujian</h2></x-slot>
    <div class="py-10"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-lg bg-white shadow-sm"><div class="overflow-x-auto p-6"><table class="min-w-full divide-y divide-slate-200 text-left text-sm"><thead class="text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-3">Ujian</th><th class="px-4 py-3">Kelas</th><th class="px-4 py-3">Peserta</th><th class="px-4 py-3">Nilai</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Mulai</th><th class="px-4 py-3">Selesai</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($attempts as $attempt)<tr><td class="px-4 py-3 font-medium">{{ $attempt->exam->nama }}</td><td class="px-4 py-3">{{ $attempt->exam->kelasData?->nama_kelas ?? $attempt->exam->kelas }}</td><td class="px-4 py-3">{{ $attempt->siswa->nama }}</td><td class="px-4 py-3">{{ $attempt->nilai_akhir ?? 'Belum Dinilai' }}</td><td class="px-4 py-3">{{ ucfirst($attempt->status) }}</td><td class="px-4 py-3">{{ $attempt->started_at?->format('d/m/Y H:i') ?? '-' }}</td><td class="px-4 py-3">{{ $attempt->submitted_at?->format('d/m/Y H:i') ?? '-' }}</td></tr>@empty<tr><td colspan="7" class="px-4 py-10 text-center text-slate-500">Belum ada hasil ujian.</td></tr>@endforelse</tbody></table><div class="mt-6">{{ $attempts->links() }}</div></div></div></div></div>
</x-app-layout>